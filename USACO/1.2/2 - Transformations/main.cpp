/*
 ID: h1994st2
 PROG: transform
 LANG: C++
 */
#include <cstdio>
#include <cstring>
#include <iostream>

#define MAX 11

using namespace std;

int n;

int main() {
    ios::sync_with_stdio(false);
    freopen("transform.in", "r", stdin);
    freopen("transform.out", "w", stdout);
    
    scanf("%d", &n);

    
    
    return 0;
}
